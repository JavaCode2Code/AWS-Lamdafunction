export const handler = async (event) => {
  // TODO implement
  const response = {
    statusCode: 200,
    body: JSON.stringify('Hello from Get Lambda!'),
  };
  return response;
};


arn in api gatway

arn:aws:lambda:ap-south-1:aws-user_id:function:scjpApiGet:alias-name


export const handler = async (event) => {
  // TODO implement
  const response = {
    statusCode: 200,
    body: JSON.stringify('Hello from Get Lambda! v1'),
  };
  return response;
};


arn in api gatway get version1

arn:aws:lambda:ap-south-1:aws-user_id:function:scjpApiGet:alias-name

in api gatway we have to give arn with alias name 
after changing version it will change lamda code

Weighted alias ---transfer load between version